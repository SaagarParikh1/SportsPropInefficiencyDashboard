# Raw Data Templates

The app supports local CSV or parquet uploads for three datasets:

- `historical_props_template.csv`
- `game_results_template.csv`
- `current_props_template.csv`

These templates contain the required headers for the MVP schema. Optional columns can be added later as long as the required fields remain present.
