services:
  - type: web
    name: sports-prop-inefficiency-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.address=0.0.0.0 --server.port=$PORT
    envVars:
      - key: THE_ODDS_API_KEY
        sync: false
